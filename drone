#include <stdio.h>
#include <stdlib.h>

int main()
{
	float x, y;
	
    printf("digite as cordenadas x, y de onde o drone ira pousar(de espaco entre 2 cordenadas quando for digitar): ");
    scanf("%f %f", &x, &y);
    
    int Aazul = (x >= 4 && x <= 14)&&(y >= 4 && y<=10);
    int Avermelha = (x >= 16 && x <= 26)&&(y >= 2 && y <= 12);
    
    if(Aazul || Avermelha ){
    	printf("Area permitida");
	}
	else{
		printf("Area proibida");
	}
    
    
	    return 0;
}
